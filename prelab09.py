#! /usr/bin/env python2.6

import sys
import os
import re

(\s[aa]a?a?a?\s)

re.sub(r"([0-9]+\.[0-9]+)|([0-9]+)|(\.[0.9]+)", "float", argo)

out = re.subn(r"([0-9]+\.[0-9]+)|([0-9]+)|(\.[0.9]+)", "float", argo)
out[1]


nums = re.findall(r"[0-9]+", argo)
final = sum(nums)

re.subn(r"EE364", "EE461", argo, 1)

ip = re.findall(r"[0-9]+", argo)
for I in ip:
	if int(I) <= 0  || int(I) >= 255 || bad == 1:
		bad = 1
		


